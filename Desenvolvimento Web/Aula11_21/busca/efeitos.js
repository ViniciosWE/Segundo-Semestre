
window.onload = configura;

function configura() {

	/* Pintar o fundo dos parágrafos todos de verde */
	//$("p").css("background-color","green");

	/* Pintar o fundo dos elementos de classe "a" de verde */
	//$(".a").css("background-color","green");

	/* Pintar o fundo dos elementos de classe "b" de verde */
	//$(".b").css("background-color","green");

	/* Pintar o fundo dos elementos de classe "a" que aparecem dentro de um div de verde */
	//$("div .a").css("background-color","green");

	/* Pintar o fundo dos elementos de id "a" e id "b" de verde */
	//$("#a, #b").css("background-color","green");

	/* Pintar o fundo dos divs que contenham o atributo "id" */
	//$("div[id]").css("background-color","green");

	/* Pintar o fundo dos parágrafos de classe "a" de verde */
	//$("p[class='a']").css("background-color","green");

	/* Pintar o fundo dos parágrafos de classe diferente de "a" de verde */
	//$("p[class!='a']").css("background-color","green");

	/* Pintar o fundo dos divs de classe "a" e id "a" de verde */
	//$("div[class='a'][id='a']").css("background-color","green");

	/* Pintar o fundo dos parágrafos que contenham o texto "texto" */
	//$("p:contains(texto)").css("background-color","green");

	/* Pintar o fundo dos parágrafos que contenham um elemento "em" interno de verde */
	//$("p:has(em)").css("background-color","green");

	/* Pintar o fundo dos parágrafos de classe "a" internos a div classe "b" de verde */
	//$("div[class='b'] p[class='a']").css("background-color","green");

	/* Pintar o fundo dos divs de classe "b" que contenham parágrafos classe "a" de verde */
	//$("div[class='b']:has(p[class='a'])").css("background-color","green");

	/* Pintar o fundo dos divs pares de verde */
	//$("div:even").css("background-color","green");

	/* Pintar o fundo do último div de verde */
	//$("div:last").css("background-color","green");


}

